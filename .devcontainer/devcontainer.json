{
	"name": "Default Linux Universal",
	"image": "mcr.microsoft.com/devcontainers/universal:5-linux",
	"features": {
		"ghcr.io/devcontainers/features/aws-cli:1": {}
	},
	"customizations": {
		"vscode": {
			"extensions": [
				"github.copilot",
				"github.copilot-chat",
				"AmazonWebServices.aws-toolkit-vscode",
				"kddejong.vscode-cfn-lint"
			]
		}
	},
	"containerEnv": {"AWS_CONFIG_FILE": "${containerWorkspaceFolder}/.devcontainer/config"},
	"postStartCommand": "npm install --global aws-cdk && pip install boto3[crt] && curl -fsSL https://cli.kiro.dev/install | bash",
	"postAttachCommand": "aws sso login"
}